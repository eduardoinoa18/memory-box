import React from 'react';
import '../styles/globals.css';

// Clean, simple Next.js app wrapper for Memory Box Admin MVP
export default function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}
